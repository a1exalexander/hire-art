import { SocialButton } from './SocialButton';

export default {
  title: 'SocialButton/SocialButton',
  component: SocialButton,
};

const Template = (args) => <SocialButton {...args} />;

export const Default = Template.bind({});
Default.args = {};
